{
  "name": "@volcano-plugins/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared helpers for Volcano IDE plugins: base-URL resolution, CLI runner, runtime content fetcher.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo"
  }
}
